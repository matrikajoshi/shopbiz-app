server:
  port: 8081
  servlet:
    context-path: /coupon-service

spring:
    application:
        name: coupon-service
    data:
      mongodb:
        host: localhost
        port: 27017
        database: coupondb

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/